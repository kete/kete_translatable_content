<%= render "topics/content_wrapper_start", :style_classes =>"full-width notabs" %>

<table style="width:100%;">
  <% untranslated_values_with_localized_labels.each do |translatable_value| %>
    <tr><%= render 'translatable_value', :translatable_value => translatable_value -%></tr>
  <% end %>
</table>

<%= link_to(t('translations.show.edit'), { :id => h(@translation.locale), :action => :edit }) %> |
<%= link_to(t('translations.show.translations'), { :action => :index }) %>
