<% @button_text = t("translations.edit.update") -%>

<% args = @translation.new_record? ? {} : {:url => { :action => :update,
  @translatable_key => @translated, :id => @translation.locale }}%>
<% form_for(@translation, args) do |f| %>

<%= render :partial => f -%>

<% end %>
